---
title: requestAnimationFrame
prefix: rw|animate
description: requestAnimationFrame
---

function loop() {
	// Work to be done each loop.

	// Update this to be a real condition
	if ( true ) {
		requestAnimationFrame( showTime );
	}
}

// kick it all off
requestAnimationFrame( loop );
